.card
  img(src=item.imageUrl, alt="Card Image")
  p.centered-text= item.text

//- Include the card CSS
style.
  .card:hover .card-image {
    opacity: 1; /* Keep image visible */
  }

  .card:hover .centered-text {
    display: none; /* Keep text hidden */
  }
